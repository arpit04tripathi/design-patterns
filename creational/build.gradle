plugins {
    id 'buildlogic.java-application-conventions'
}

group = 'com.a04t.dp'
version = '0.0.1'

dependencies {
//    api project(':common')            // api only allowed if java plugin applied
    implementation project(':common')
}

application {
    // Define the main class for the application.
    mainClass = 'com.a04t.dp.creational.Main'
}
